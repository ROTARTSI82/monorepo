this is an astro blog using bun. i am working on writing a blog post (@src/markdown/2026/bytegpt.mdx) and i would like to include visualizations of my models weights in it. i had written some python code (@local/reference/infer.py and @local/reference/charmodel.py in `visualize()`) that outputs some png representations of my models weights, and i would like something similar to this (maybe you can find a better algo for mapping weights to colors).

specifically, i want you to write a component in src/components/2026/ that i can embed into my blog post. It should be clean, as simple as possible, and match the minimalistic style of the rest of my blog. there should be a dropdown to select the tensor one wants to view (tok_embed, tok_embed.row_corr, tok_embed.col_corr, layer.n.attn.wk, layer.n.mlp.w2, etc), and it should render out the image. the model weights are stored in src/assets/2026/bytegpt/model_linux.bin, in a binary format specified by the CharModel struct in @local/reference/src/model.cpp.

This widget should allow the user to zoom in/pan and hover over the pixels to see their FP32 values. It should also show the coordinates of the thing you're hovering, and in the case of the tok_embed matrix it should show the name of the byte it corresponds to (epsecially useful to see what you're dotting in tok_embed.row_corr and tok_embed.col_corr). The widget should also display some stats about the tensor, like mean, stddev, min, max, median/25+75quantiles possibly? Display them in a compact format like mu \pm 1/sqrt(n) * stddev, then [min, q1, med, q3, max] or something. For the attention weights, since there are 8 heads, possibly compute these for each head independently? or report row/col stats. Since there are some bytes that rarely or never appear in the dataset (see @local/reference/bytedata.txt for a report of the counts of bytes appearing in the dataset and an example of the models generated text), you might want to include some indication of this in visualizations for the tok_embed* tensors: the coordinates can indicate the percentage of the training data that the byte takes or some other metric (probability that the byte appears in a sample of X characters?). an idea i had was also adding a threshold slider and computing the stats based on the submatrix you get when you remove the bytes that dont meet the threshold.

you do not have to implement every single idea i mention (avoid info overload). choose what will result in the best experience for exploring the model weights.

In the future, i would also like to add visualizations of the activations from each layer to the demo at src/pages/bytegpt.astro , turning the d_model activations into 16x16 images. do not implement anything for this, but structure your code in a modular way so that implementing this will be easy in the future (split things out into components and scripts, and organize your code neatly).
