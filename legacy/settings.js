const showDelay = 1250;
const qTimeout = 8000;
const buzzTimeout = 8000;

const coinMultiplier = 1000;

const newMsg = false;


const anchor =  1000 * 1000 * 25;  // Base amount is A$1 billion

module.exports = {
    showDelay, qTimeout, buzzTimeout, coinMultiplier, anchor, newMsg
}
